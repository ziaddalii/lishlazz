"use client";

import {Button, Tooltip} from "@mui/material";
import {ReactNode} from "react";

interface Props {
    icon: string;
    label: string;
}

export default function ContactButton({icon, label}: Props) {
    return (
            <Button color="primary">
            </Button>
    );
}
